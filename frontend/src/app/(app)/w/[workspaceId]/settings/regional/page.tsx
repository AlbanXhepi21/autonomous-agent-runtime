import { RegionalSettings } from "@/features/settings/regional-settings";

export const metadata = { title: "Regional & data — Settings" };

export default function RegionalSettingsPage() {
  return <RegionalSettings />;
}
