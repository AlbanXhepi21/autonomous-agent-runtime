import { DataSourcesSettings } from "@/features/settings/data-sources-settings";

export const metadata = { title: "Data Sources — Settings" };

export default function DataSourcesSettingsPage() {
  return <DataSourcesSettings />;
}
