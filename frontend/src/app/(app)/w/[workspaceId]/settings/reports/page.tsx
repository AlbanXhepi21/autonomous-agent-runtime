import { ReportPreferencesSettings } from "@/features/settings/report-preferences-settings";

export const metadata = { title: "Report preferences — Settings" };

export default function ReportPreferencesSettingsPage() {
  return <ReportPreferencesSettings />;
}
